# 13 — Manual test plan (คลิกจริง หา bug ก่อนเขียนโค้ดต่อ)

ชั้น HTTP/UI คือชั้นเดียวที่ยังไม่เคยถูกทดสอบจริง — DB, RPC และ parser ผ่านสคริปต์หมดแล้ว
(ดู [11 — Verification](11-verification.md)) หน้านี้คือลำดับคลิกที่ครอบ flow เงินทั้งหมด
ทำตามลำดับ ห้ามข้าม เพราะขั้นหลังใช้สถานะจากขั้นก่อน

**เวลาที่ใช้:** ~45 นาที (ไม่รวมส่วนที่ต้องใช้มือถือ)

---

## 0. ก่อนเริ่ม

| | |
|---|---|
| ฐาน | pilot `vltzkxmblmrvsmaookhl` — **ทุกอย่างที่กดเขียนลงฐานนี้จริง** |
| Admin | `canvasmediaagency@gmail.com` · รหัสตั้งใหม่ 2026-09-14 (ไม่อยู่ในรีโป) · role `super_admin` |
| รอบล่าสุด | **2026-09-14 ทำขั้น 1–6 + 8.3 บน production แล้ว** — เงินไม่พังสักจุด · ผลรายขั้นใน `docs/TEST_RUN_2026-09-14.md` (ไม่อยู่ใน git) · สรุปบั๊กใน [09 — Open debt](09-status-and-roadmap.md#open-debt) · เหลือขั้น 7, 8.1–8.2 (ต้องมือถือ) และ 9 |
| Admin ทดสอบ | ยังไม่มีบนฐานใหม่ — สร้าง role `accounting` ใหม่ถ้าจะทดสอบ 403 ซ้ำ |
| Deploy | https://pilot-phase1.vercel.app |
| Local | `npm run dev` → http://localhost:3000 · ชี้ฐาน pilot ตัวเดียวกัน |

ทดสอบฝั่ง **admin ที่ local** (เห็น console/network ตอนพัง) · ฝั่ง **ลูกค้าที่ deploy**
(LIFF ต้องเปิดจากแอป LINE เท่านั้น)

```bash
node scripts/verify-demo-ready.js   # ต้องได้ 19/19 ก่อนเริ่ม
npm run dev
```

### สถานะฐานตอนเริ่ม (ฐานใหม่ `vltzkxmblmrvsmaookhl` · ตรวจแล้ว 14 ก.ย. 2026)

- ลูกค้าสาธิต 8 คน แต้ม 0 ทุกคน · **ยังไม่มีลูกค้า LINE จริง** — ต้องสมัครผ่าน LIFF ใหม่ 1 คนก่อนทดสอบขั้น 7 (OTP ใช้ได้เบอร์ทดสอบเดียว)
- แคมเปญ 3 ช่วง: 1.5× มิ.ย. · **2× 23 ก.ค.–5 ส.ค.** · 1.5× 15 ส.ค.–31 ธ.ค. (active วันนี้)
- พนักงานขาย 4 คน S01–S04
- รางวัล 3 ชิ้น — **ยังไม่มีรูปทั้ง 3**
- `point_batches` ว่าง · ไม่มี redemption

### จดผลไว้แบบนี้

```
[ขั้นที่ N] สิ่งที่กด → สิ่งที่เห็น → คาดหวังอะไร → 🐛/✅
```

---

## 1. Auth และ RBAC (5 นาที)

| # | ทำ | คาดหวัง |
|---|---|---|
| 1.1 | เปิด `/admin/batches` โดยยังไม่ล็อกอิน | เด้งไป `/admin/login` ไม่ใช่หน้าเปล่าหรือ error |
| 1.2 | ล็อกอินด้วยรหัสผิด | ข้อความไทยอ่านรู้เรื่อง ไม่ใช่ error ดิบจาก Supabase |
| 1.3 | ล็อกอินถูก | เข้า `/admin` เห็นตัวเลข dashboard ไม่ใช่ 0 ทั้งหมด |
| 1.4 | กด refresh ที่ `/admin/batches` | ยังอยู่ในระบบ ไม่เด้งออก |

ระหว่างนี้เปิด DevTools → Console ค้างไว้ ทุกขั้นถัดไปให้เหลือบดู error สีแดง

```bash
# ยืนยันว่า API ปิดจริงตอนไม่มี session
curl -s -o /dev/null -w "%{http_code}\n" https://pilot-phase1.vercel.app/api/admin/batches       # 401
curl -s -o /dev/null -w "%{http_code}\n" https://pilot-phase1.vercel.app/api/admin/users         # 401
curl -s -o /dev/null -w "%{http_code}\n" https://pilot-phase1.vercel.app/api/admin/sales-reps    # 401
```

---

## 2. พนักงานขาย (5 นาที) — `/admin/sales-reps`

| # | ทำ | คาดหวัง |
|---|---|---|
| 2.1 | เปิดหน้า | เห็น 4 คน S01–S04 ทั้งหมด active |
| 2.2 | เพิ่มคนใหม่ รหัส `s01` (ตัวเล็ก) | **ต้องถูกปฏิเสธ** — รหัสซ้ำแบบไม่สนตัวพิมพ์ ถ้าเพิ่มได้ = 🐛 |
| 2.3 | เพิ่มคนใหม่ รหัส `S99` ชื่อ `ทดสอบ ลบทีหลัง` | เพิ่มได้ ขึ้นในรายการทันที |
| 2.4 | ปิดการใช้งาน `S99` | สถานะเปลี่ยนเป็นปิด และ **ไม่มีปุ่มลบ** (ตั้งใจ — ledger อ้างถึงอยู่) |
| 2.5 | เว้นชื่อว่างแล้วกดบันทึก | ขึ้น error ไม่ใช่บันทึกชื่อว่าง |

---

## 3. Template (3 นาที) — `/admin/batches`

| # | ทำ | คาดหวัง |
|---|---|---|
| 3.1 | กดดาวน์โหลด template | ไฟล์ `.xlsx` เปิดได้ ไม่พัง |
| 3.2 | ดูคอลัมน์พนักงานขาย | dropdown มี S01–S04 · **ไม่มี S99 ที่เพิ่งปิด** ถ้ามี = 🐛 |
| 3.3 | ดูคอลัมน์เบอร์โทร | เป็นรูปแบบข้อความ พิมพ์ `0812345678` แล้ว 0 ตัวหน้าไม่หาย |

---

## 4. อัปโหลด → preview (10 นาที) — จุดที่ต้องละเอียดที่สุด

ไฟล์: `docs/demo/demo-ยอดขาย-2026-07-20_2026-07-26.xlsx` · สัปดาห์ **2026-07-20 → 2026-07-26**

| # | ทำ | คาดหวัง |
|---|---|---|
| 4.1 | อัปโหลดโดย**ไม่**เลือกสัปดาห์ | error บอกให้เลือกสัปดาห์ ไม่ใช่ 500 |
| 4.2 | อัปโหลดไฟล์ที่ไม่ใช่ .xlsx (เช่น .png) | ถูกปฏิเสธพร้อมข้อความอ่านรู้เรื่อง |
| 4.3 | เลือกสัปดาห์ **2026-08-03 → 2026-08-09** (ผิดสัปดาห์) แล้วอัปไฟล์ demo | ทุกแถวต้องขึ้นผิดพลาด "วันที่ซื้อนอกสัปดาห์" — ถ้าผ่านเขียว = 🐛 ร้ายแรง |
| 4.4 | เลือกสัปดาห์ถูก `2026-07-20 → 2026-07-26` แล้วอัปไฟล์ demo | **8 เขียว · 1 เหลือง · 1 แดง · รวม 687 แต้ม** |
| 4.5 | ไล่ดูรายแถวเทียบตารางใน `docs/demo/README.md` | 25 / 105 / 48 / 9 / 120 / 300 / 60 / 20 · แถว 10 เหลือง (เบอร์ 0899999999) · แถว 11 แดง (ลดหนี้ > ยอดซื้อ) |
| 4.6 | ดูว่าแถวไหนได้ ×2 | เฉพาะ 23–26 ก.ค. — ถ้า 20–22 ก.ค. ได้ ×2 ด้วย = แคมเปญเพี้ยน (เคยเกิดมาแล้ว) |
| 4.7 | อัปไฟล์เดิมซ้ำทันที (ยังไม่ยืนยัน) | **อัปได้ และ preview ใหม่ทับ preview เดิม** (ประวัติเหลือแถวเดียว) — ตั้งใจ: `upload/route.ts` ลบ batch `previewed`/`draft` ที่ sha256 ตรงกันก่อนสร้างใหม่ เพื่อให้เลือกสัปดาห์ผิดแล้วอัปใหม่ได้ · sha256 กัน (409) **เฉพาะ** ไฟล์ที่ `committed` แล้ว — ถ้าโดน 409 ตรงนี้ = 🐛 |
| 4.8 | ปิดหน้าแล้วเปิด `/admin/batches` ใหม่ | batch ยังอยู่ status `previewed` — preview ไม่หายไปเฉย ๆ · ⚠️ รู้แล้ว (2026-09-14): แถวนี้**ไม่มีปุ่มยืนยัน** ต้องอัปไฟล์เดิมซ้ำ (ข้อ 4.7) ถึงจะ commit ได้ |

> ⚠️ ขั้น 4.3 กับ 4.7 จะสร้าง `point_batches` แถวขยะเพิ่ม ปกติ — ลบทิ้งตอนขั้น 9

---

## 5. ยืนยัน → แต้มเข้า (10 นาที) — จุดที่เงินเคลื่อนจริง

| # | ทำ | คาดหวัง |
|---|---|---|
| 5.1 | กดปุ่มยืนยัน "ให้แต้ม 687 แต้ม" | สำเร็จภายในไม่กี่วินาที ไม่ค้าง |
| 5.2 | **กดปุ่มยืนยันรัวสองครั้ง** (double-click) ถ้ากดทัน | ต้องเข้าครั้งเดียว — ถ้าแต้มเข้าสองรอบ = 🐛 ร้ายแรงที่สุดในหน้านี้ |
| 5.3 | `/admin/users` ดูลูกค้าสาธิต 8 คน | สมชาย 25 · ปราณี 105 · ธนากร 48 · วรรณภา 9 · อนุชา 120 · จันทร์เพ็ญ 300 · ประเสริฐ 60 · มาลี 20 |
| 5.4 | รวมแต้มทั้ง 8 คน | = 687 พอดี |
| 5.5 | เปิดลูกค้าคนหนึ่ง ดูประวัติแต้ม | เห็นรายการ `earned` ผูกกับเลขบิล DM-2607-xxx |
| 5.6 | `/admin/batches` ดูประวัติ | status `committed` · โชว์ **"อัปโหลดโดย"** และ **"แต้มเข้าโดย"** แยกช่องกัน ถ้าช่องใดว่าง = 🐛 |
| 5.7 | กดเข้าไปดูรายละเอียด batch | ~~ยังไม่มีหน้านี้~~ ตั้งแต่ 9R มี `GET /api/admin/batches/:id` + ปุ่ม "เปิด preview" / "ตรวจ/อนุมัติ" — ดู §10 |

---

## 6. ยกเลิก batch (5 นาที) — ทางกลับของเงิน

| # | ทำ | คาดหวัง |
|---|---|---|
| 6.1 | กดยกเลิก ใส่เหตุผล 1 ตัวอักษร | ถูกปฏิเสธ (ต้อง ≥ 3 ตัวอักษร) |
| 6.2 | ใส่เหตุผลจริง เช่น `ทดสอบระบบ` แล้วยืนยัน | สำเร็จ |
| 6.3 | `/admin/users` ดูลูกค้าสาธิตทั้ง 8 | กลับเป็น **0 แต้มทุกคน** เหลือค้าง 1 แต้มก็ = 🐛 |
| 6.4 | ดูประวัติแต้มของลูกค้า | มีรายการคืนแต้ม ไม่ใช่แค่ตัวเลขลดลงเฉย ๆ |
| 6.5 | `/admin/batches` | status `voided` · โชว์คนยกเลิก + เหตุผล (แก้แล้วใน 9R) |
| 6.6 | อัปไฟล์ demo เดิมซ้ำอีกครั้ง | **ต้องอัปได้แล้ว** — void ปลดล็อกเลขบิล ถ้ายังโดน sha256 กัน = 🐛 (ออกแบบไว้ให้อัปซ้ำได้หลัง void) |

> หลัง 6.6 ถ้าอัปได้ ให้ **ยกเลิก batch ใหม่ทิ้งด้วย** จะได้กลับสู่สถานะเริ่มต้น

---

## 7. ฝั่งลูกค้า — ต้องใช้มือถือจริง (10 นาที)

เปิดจากแอป LINE เท่านั้น (LIFF `2010850077-4iXzuV6k`)

| # | ทำ | คาดหวัง |
|---|---|---|
| 7.1 | สมัครลูกค้าใหม่ผ่าน LIFF (เบอร์ทดสอบ OTP) แล้วปรับแต้มมือ +400 ที่ `/admin/users` → เปิด LIFF | เห็นแต้ม **400** ตรงกับ `/admin/users` |
| 7.2 | หน้าแลกรางวัล | เห็นรางวัล 3 ชิ้น · 2 ชิ้นไม่มีรูป (รู้อยู่แล้ว) ดูว่า layout พังไหม |
| 7.3 | แลก `หมวก Hughome` (100 แต้ม) | สำเร็จ · แต้มเหลือ 300 ทันที |
| 7.4 | แลกซ้ำจนแต้มไม่พอ | ถูกปฏิเสธพร้อมข้อความ ไม่ใช่แต้มติดลบ |
| 7.5 | `/admin/redemptions` ฝั่ง admin | เห็นคำขอใหม่ status `รอดำเนินการ` (`requested`) พร้อม **รหัสรับของ 8 ตัว** (ต้อง apply `023` ก่อน ไม่งั้นช่องรหัสว่าง) |
| 7.6 | กดอนุมัติ | status เปลี่ยน · สต็อกรางวัลลด 1 |

### 7b. Sprint 8 — 4 สถานะ + QR + แจ้งทีม (10 นาที) · เขียน 2026-09-14 ยังไม่ได้คลิก

ก่อนเริ่ม: `023` apply แล้ว (14 ก.ย. 2026) · ตั้ง `NOTIFY_TOKEN_KEY` บน Vercel (เฉพาะถ้าจะใช้ Telegram) ·
**`NOTIFICATIONS_ENABLED` บน Vercel production เป็น `false`** — LINE group / LINE push ทุกชนิดจะ "สำเร็จ" แต่ไม่มีอะไรส่งจริง
จนกว่าจะตั้ง `true` แล้ว redeploy · ชั้น API ของ §7b ผ่านแล้วบน prod ด้วย `node scripts/e2e-sprint8-http.js --yes` (50/51) —
ขั้นที่เหลือคือ UI + มือถือจริง

| # | ทำ | คาดหวัง |
|---|---|---|
| 7b.1 | มือถือลูกค้า: หลังกดแลก (7.3) | เด้ง dialog **QR รับของ** + รหัส 8 ตัว · แท็บประวัติมีปุ่มรหัสเดียวกัน · ข้อความใต้รายการ "ร้านกำลังตรวจสอบคำขอ" |
| 7b.2 | `/admin/notifications` → เพิ่ม Telegram (bot token + chat id กลุ่ม) → **ทดสอบส่ง** | ข้อความทดสอบเข้ากลุ่มภายใน 5 วิ · หน้า admin โชว์ token แค่ `123456789:••••••••xxxx` · `GET /api/admin/notifications` ไม่มี token จริง |
| 7b.3 | ลูกค้ากดแลกอีกใบ | ข้อความ "มีคำขอแลกรางวัลใหม่" เข้ากลุ่ม: ชื่อ เบอร์ ของ แต้ม รหัสรับของ ลิงก์ `/admin/redemptions` |
| 7b.4 | ปิด channel (switch) แล้วแลกอีกใบ | **ไม่มี**ข้อความเข้ากลุ่ม · การแลกยังสำเร็จ |
| 7b.5 | ใส่ token ผิด (กดกุญแจ → กรอกมั่ว) → ทดสอบส่ง | toast แดง "Telegram 401: Unauthorized" · ช่อง "ส่งล่าสุด" โชว์ "ล้มเหลว: …" · การแลกของลูกค้า**ยังสำเร็จ**แม้ notify ล้ม |
| 7b.6 | `/admin/redemptions` ใบ `รอดำเนินการ` → **อนุมัติ** → **ของพร้อมรับ** | ผ่านทีละขั้น · แท็บกรองตามสถานะทั้ง 5 ใช้ได้ · มือถือลูกค้า refresh เห็น "มารับได้ที่หน้าร้าน แสดง QR ที่เคาน์เตอร์" |
| 7b.7 | มือถือพนักงาน (ยังไม่ login admin) ใช้กล้องสแกน QR ของลูกค้า | เปิด `/admin/login?next=/admin/redemptions/scan?code=…` → login แล้วเด้งกลับหน้าสแกน พร้อมใบแลกขึ้นมาให้ทันที |
| 7b.8 | กด **ยืนยันส่งมอบของ** | สถานะ `รับของแล้ว` · `delivered_by` / `delivered_at` มีค่า · หน้าลูกค้าไม่มีปุ่ม QR แล้ว |
| 7b.9 | ใบที่ `รับของแล้ว` — หาปุ่มยกเลิก | **ไม่มีปุ่ม** · ยิง `POST /api/admin/redemptions/:id/cancel` ตรง ๆ ต้องได้ 400 "only requested/approved/ready can be cancelled" |
| 7b.10 | ยกเลิกใบที่ `อนุมัติแล้ว` หรือ `พร้อมรับของ` | แต้มคืนเข้าก้อนเดิม (`redemption_lots`) · สต็อกคืน · ประวัติแต้มมีแถว refund · `points_balance == SUM(ledger)` (รัน `e2e-points-invariant.js --yes` หรือ cron reconcile) |
| 7b.11 | สแกน/พิมพ์รหัสของใบที่ยัง `รอดำเนินการ` ที่ `/admin/redemptions/scan` | ไม่มีปุ่มส่งมอบ · บอกว่า "ต้องเป็นพร้อมรับของก่อน" พร้อมปุ่มเดินหน้าทีละขั้น |
| 7b.12 | พิมพ์รหัสมั่ว `ABCD0000` | 400 "รหัสรับของต้องเป็นตัวอักษร/ตัวเลข 8 ตัว" (0/O/1/I ไม่อยู่ในชุด) · รหัสถูกรูปแบบแต่ไม่มี → 404 |
| 7b.13 | สองคนกด "อนุมัติ" ใบเดียวกันพร้อมกัน | คนหลังได้ 409 "สถานะถูกเปลี่ยนโดยคนอื่นไปแล้ว" ไม่ใช่สำเร็จซ้อน |

---

## 8. ยืนยัน bug ที่อ่านโค้ดเจอแล้ว (5 นาที)

> **แก้ในโค้ดแล้ว 2026-09-13** (`cancel_redemption` RPC · actor จาก session · `adjust_points_manual` สร้าง lot) — ขั้นนี้เหลือไว้เพื่อยืนยันที่ UI · 8.3 ยืนยันแล้ว 2026-09-14 (+50/−50 ผ่าน) · 8.1/8.2 ยังไม่ได้คลิกเพราะต้องมี redemption จากขั้น 7 ก่อน

3 ข้อนี้พบจากการอ่านโค้ดกับข้อมูลในฐาน **ก่อน**เริ่มคลิก — ทำขั้นนี้เพื่อดูว่าอาการโผล่ที่ UI ยังไง

| # | ทำ | ที่คาดว่าจะเจอ |
|---|---|---|
| 8.1 | `/admin/redemptions` → ยกเลิกคำขอที่ยัง `requested` (ใช้ใบจาก 7.3 ก็ได้) | แต้มคืนเข้า `points_balance` **แต่ไม่มีแถวคืนแต้มในประวัติ** — `/api/admin/redemptions/[id]/cancel` insert ลง `point_transactions` ด้วยคอลัมน์ `reference_type` / `reference_id` ที่ไม่มีจริงในตาราง แล้ว error ถูกกลืน (`// Don't fail the operation if transaction log fails`) |
| 8.2 | ดูช่อง "ดำเนินการโดย" ของใบที่ยกเลิก | ว่าง — route อ่าน `adminId` จาก body ที่ client ไม่ได้ส่งมา แทนที่จะอ่านจาก session (ใบเก่าในฐานก็ `processed_by: null`) |
| 8.3 | `/admin/users` → ปรับแต้มมือ +50 ให้ลูกค้าสาธิตคนหนึ่ง | ยอดขึ้น 50 แต่ไม่มี lot ใน `point_batch_ledger` → ตอนทำ FIFO/หมดอายุ (Sprint 7) แต้มก้อนนี้จะไม่มีวันหมดอายุและหักไม่ได้ · **ถ้าได้ "Failed to adjust points" (500)** ให้ดู Vercel log ก่อนโทษ RPC — 2026-09-14 เจอ 2 ครั้งติด สาเหตุคือ Supabase `GET /auth/v1/user` 504 ใน `requirePermission` ครั้งที่ 3 ผ่าน |

หลักฐานที่มีอยู่แล้วในฐาน: ลูกค้า `JayJayMe!!` ยอด **400** แต่ผลรวม `point_transactions` = **300**
และ `point_batch_ledger.points_remaining` = **300** — ต่างกัน 100 พอดีกับใบที่ยกเลิกไป

ทั้ง 3 ข้อมีรากเดียวกัน: **เงินถูกขยับด้วย `UPDATE` ตรง ๆ ใน API route แทนที่จะผ่าน RPC**
(`cancel/route.ts:61`, `users/[id]/points/route.ts:73`) ซึ่งขัดกับกติกาข้อ 2 ใน `CLAUDE.md`
`redeem_reward` / `award_points_from_batch` / `void_batch` ทำถูกอยู่แล้ว — 2 เส้นนี้คือที่หลุด

---

## 9. เก็บกวาดหลังทดสอบ

```bash
node scripts/verify-demo-ready.js   # ต้องกลับมา 19/19 ถ้าไม่ใช่ แปลว่ามีอะไรค้าง
```

ถ้าไม่ผ่าน แปลว่ายัง void batch ไม่ครบ หรือมีแถวขยะจากขั้น 4.3 / 4.7 — ลบใน SQL Editor:

```sql
-- batch ที่ยังไม่ commit เท่านั้น ปลอดภัยที่จะลบ
DELETE FROM point_batches WHERE status = 'previewed';
-- พนักงานทดสอบจากขั้น 2.3 (ลบได้ถ้าไม่มี ledger อ้างถึง)
DELETE FROM sales_reps WHERE code = 'S99';
```

แต้มที่ปรับมือในขั้น 8.3 ต้องปรับกลับด้วยมือเช่นกัน

---

## 10. Approval flow — Sprint 9R (15 นาที) · เขียน 2026-09-21 ยังไม่ได้คลิก

> ต้อง apply migration `024` + `025` ก่อน · ใช้ไฟล์ demo ที่ regenerate แล้ว (spec v2 · 9 คอลัมน์ · คอลัมน์ A รหัสลูกค้าเว้นว่าง)
> ข้อ 4–6 ข้างบนเปลี่ยนไป: ปุ่ม "ยืนยัน — ให้แต้ม" ไม่มีแล้ว ต้องผ่าน "ส่งให้ผู้อนุมัติ" → "อนุมัติ (แต้มเข้า)"
> ต้องมี 2 บัญชี: `accounting` (บัญชี) และ `manager` (ผู้อนุมัติ) — super_admin ทำได้ทั้งคู่แต่จะพิสูจน์สิทธิ์ไม่ได้

| # | ทำ | คาดหวัง |
|---|---|---|
| 10.1 | login เป็น **accounting** → `/admin/batches` อัปไฟล์ demo สัปดาห์ 2026-07-20 → 07-26 | preview: ใช้ได้ 8 · ผิดพลาด 1 · ไม่พบลูกค้า 1 · รวม 687 · มีคอลัมน์ "รหัสลูกค้า" (ว่างทุกแถว ไม่มี warning) · หัวคอลัมน์ "Maker" |
| 10.2 | ดูปุ่มใต้ตาราง | เห็นเฉพาะ **"ส่งให้ผู้อนุมัติ — 8 แถว · 687 แต้ม"** · ไม่มีปุ่มอนุมัติ |
| 10.3 | reload หน้า → ประวัติ → แท็บ "รอส่ง" → กด "เปิด preview" | preview เดิมกลับมาครบ 10 แถว (fix debt เก่า) |
| 10.4 | กด "ส่งให้ผู้อนุมัติ" | toast สำเร็จ · สถานะเป็น **รอผู้อนุมัติ** · `/admin/users` แต้มยัง 0 ทุกคน (ยังไม่มีเงินขยับ) · ช่อง "ส่งโดย" มีชื่อบัญชี |
| 10.5 | (ยัง accounting) เรียก `POST /api/admin/batches/<id>/commit` ผ่าน DevTools/curl | **403** — accounting ไม่มี `batches.approve` แล้ว |
| 10.6 | อัปไฟล์ demo เดิมซ้ำ | **409** "ถูกส่งให้ผู้อนุมัติไปแล้ว" (ไม่ทับ preview เหมือนตอน previewed) |
| 10.7 | login เป็น **manager** → `/admin/batches` แท็บ "รอผู้อนุมัติ" → "ตรวจ/อนุมัติ" | เห็นแถวครบ · ปุ่ม **"อนุมัติ (แต้มเข้า) — 687 แต้ม"** + **"ปฏิเสธ"** · ไม่มีปุ่มอัปโหลด (manager ไม่มี `batches.upload`) |
| 10.8 | กด "ปฏิเสธ" ใส่เหตุผล | สถานะ **ยกเลิก** · โชว์ "โดย <manager> · เหตุผล" · แต้มยัง 0 · ไม่มีรายการในประวัติแต้มลูกค้า · ไม่มี ledger |
| 10.9 | accounting อัปไฟล์เดิมใหม่ → ส่งให้ผู้อนุมัติ → manager กด "อนุมัติ (แต้มเข้า)" | toast "อนุมัติแล้ว — แต้มเข้า 687" · ลูกค้า 8 คนได้แต้มตามข้อ 5.3 · "อนุมัติโดย" = manager · "ส่งโดย" = accounting (คนละช่อง) |
| 10.10 | **กดอนุมัติรัวสองครั้ง** ถ้ากดทัน | เข้าครั้งเดียว — รอบสองต้อง 409 |
| 10.11 | ดู `point_batch_ledger` ของชุดนี้ (SQL Editor) | `expires_at` = **วันนี้ + 365** ทุกแถว (ไม่ใช่ 2027-07-31) · `earned_month` = เดือนนี้ · `purchase_date` ยังเป็น ก.ค. 2026 |
| 10.12 | เปิด LIFF ของลูกค้าสาธิตที่มีแต้ม | "แต้ม X จะหมดอายุ <เดือนนี้ ปีหน้า>" |
| 10.13 | manager กด "ยกเลิกทั้งชุด (Rollback)" ใส่เหตุผล | dialog บอกชัดว่าดึงแต้ม "ทุกคน" คืน · หลังยืนยันแต้มกลับ 0 ทุกคน · มีรายการคืนในประวัติ · โชว์คนยกเลิก |
| 10.14 | `/admin/notifications` — ถ้ามี channel อยู่ ติ๊ก "มีชุดยอดขายรอผู้อนุมัติ" แล้วทำ 10.4 ใหม่ | ข้อความถึงกลุ่มภายใน 5 วิ: ชื่อผู้ส่ง สัปดาห์ ไฟล์ แถว แต้ม ลิงก์ · ถ้าไม่ติ๊ก ไม่มีข้อความ (ตามออกแบบ) |
| 10.15 | ทำไฟล์ 2 แถว: เบอร์เดียวกัน วันเดียวกัน ยอดเท่ากัน คนละเลขบิล | preview: แถวสองสีเหลือง "ยอดซ้ำกับแถว N" · badge "ยอดซ้ำ 1 แถว" · ส่งและอนุมัติได้ · แต้มเข้าทั้ง 2 แถว |
| 10.16 | ทำไฟล์ที่กรอกรหัสลูกค้ามั่วในแถวที่เบอร์ถูก | แถวยัง "ใช้ได้" แต่มี ⚠ "รหัสลูกค้าไม่ตรงกับระบบ" · แต้มเข้าตามเบอร์ |
| 10.17 | ทำไฟล์ template เก่า (8 คอลัมน์ · พนักงานขาย) | ถูกปฏิเสธทั้งไฟล์ที่หัวตาราง |

---

## 11. Export — Sprint 9R (5 นาที)

| # | ทำ | คาดหวัง |
|---|---|---|
| 11.1 | `/admin/reports` เลือกช่วง 2026-07-01 → 07-31 → "ดาวน์โหลด Excel" | ไฟล์ชื่อ `customers_2026-07-01_2026-07-31.xlsx` · แผ่น "ลูกค้า" 1 แถว/คน **ทุกคน** (ไม่ใช่แค่ที่สมัครในช่วง) |
| 11.2 | เปิดใน Excel ดูคอลัมน์ | รหัสลูกค้า · ชื่อ · นามสกุล · เบอร์โทร · ประเภท · วันที่สมัคร (พ.ศ.) · แต้มคงเหลือ · แต้มหมดอายุก้อนถัดไป + วัน · ยอดซื้อสุทธิรวมในช่วง · จำนวนบิลในช่วง · แท็ก · **ไม่มีเลขที่บิล** |
| 11.3 | เบอร์โทร | `0800000001` ขึ้นต้น 0 ไม่มีขีด · เซลล์เป็นข้อความ (ชิดซ้าย ไม่กลายเป็น 800000001) |
| 11.4 | แถวที่ 1 | ตัวหนา · freeze · autofilter · ความกว้างอ่านได้ทันที |
| 11.5 | `/admin/reports` ส่วน "รายงานชุดยอดขายรายสัปดาห์" → Excel ของชุดที่อนุมัติใน §10 | ไฟล์ `batch_2026-07-20_2026-07-26.xlsx` · 1 แถว/บิล · มี **เลขที่บิล + Maker + สาขา** · แถวท้าย "รวม 8 บิล … 687" · แผ่น "ข้อมูลชุด" มีชื่อคนอัปโหลด/ส่ง/อนุมัติ |
| 11.6 | ปุ่มเดียวกันจาก `/admin/batches` (ไอคอนไฟล์ท้ายแถว) | ไฟล์เดียวกัน |

---

## 12. สรุปผลอย่างไร

จดเป็นตารางเดียว แล้วค่อยตัดสินใจว่าจะแก้ก่อนหรือเขียน Sprint 6 ต่อ

| ขั้น | อาการ | ร้ายแรง | ไฟล์ที่น่าจะเป็นต้นเหตุ |
|---|---|---|---|

เกณฑ์: **แต้มผิด / เข้าสองรอบ / ยกเลิกแล้วไม่คืนครบ = หยุดทุกอย่างแล้วแก้ก่อน**
ส่วนข้อความ ภาษา ปุ่มเบี้ยว = จดไว้ทำรวมทีเดียวใน Sprint 9
